import type { CommandConfig } from './types'

export const commands: CommandConfig[] = [
  {
    name: "o",
    description: "Enter orchestrate mode for feature development",
    contentFile: "content/commands/orchestrate.md"
  }
]
