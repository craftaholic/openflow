import { orchestrate } from '../../content/commands/o'

export const opencodeCommands = {
  orchestrate: { name: orchestrate.name, description: orchestrate.description, trigger: orchestrate.trigger, instruction: orchestrate.instruction },
}
